/** OAuth flow: redirect back to redirect_uri with code */
export const FLOW_REDIRECT = "redirect";

/** OAuth flow: postMessage to opener window (e.g. popup) */
export const FLOW_POST_MESSAGE = "postmessage";
